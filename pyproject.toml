[project]
name = "mcp-osm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.3.0",
    "psycopg2>=2.9.10",
    "flask>=3.1.0",
]

[project.optional-dependencies]
dev = ["coverage>=6.0.0"]
